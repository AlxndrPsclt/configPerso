# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8
#font pango: Oxygen Mono:style=Regular 11
#font pango: Hack:style=Regular 11
font pango: Cousine for Powerline:style=Regular 11

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec st
bindsym $mod+o exec urxvt
bindsym $mod+i exec qutebrowser
bindsym $mod+e exec st vifmi
#bindsym $mod+a exec st -c pop-pulsemixer pulsemixer
bindsym $mod+a [class="pop-pulsemixer"] scratchpad show

bindsym $mod+t [class="pop-taskwarrior"] scratchpad show

bindsym XF86Search [class="pop-finder"] scratchpad show
bindsym $mod+space [class="pop-finder"] scratchpad show
bindsym $mod+m [class="pop-music-finder"] scratchpad show

bindsym XF86Calculator [class="pop-calculator"] scratchpad show

bindsym $mod+p [class="pop-pass"] scratchpad show

# kill focused window
bindsym $mod+Shift+q kill

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

bindsym $mod+Shift+s move workspace to output right

bindsym $mod+Tab exec i3-msg workspace next_on_output
bindsym $mod+shift+Tab exec i3-msg workspace prev_on_output

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle
bindsym $mod+n exec ~/.config/perso/focus/focus_st.sh

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+q layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
#bindsym $mod+space focus mode_toggle

bindcode $mod+49 workspace back_and_forth
bindsym $mod+49 workspace back_and_forth

# focus the parent container
#bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child
#

set $WS1 "1"
set $WS2 "2"
set $WS3 "3"
set $WS4 "4"
set $WS7 "7"
set $WS8 "8"
set $WS9 "9"
set $WS10 "10"

# switch to workspace
bindsym $mod+1 workspace $WS1
bindsym $mod+2 workspace $WS2
bindsym $mod+3 workspace $WS3
bindsym $mod+4 workspace $WS4
bindsym $mod+5 workspace $WS5
bindsym $mod+6 workspace $WS6
bindsym $mod+7 workspace $WS7
bindsym $mod+8 workspace $WS8
bindsym $mod+9 workspace $WS9
bindsym $mod+0 workspace $WS10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $WS1
bindsym $mod+Shift+2 move container to workspace $WS2
bindsym $mod+Shift+3 move container to workspace $WS3
bindsym $mod+Shift+4 move container to workspace $WS4
bindsym $mod+Shift+5 move container to workspace $WS5
bindsym $mod+Shift+6 move container to workspace $WS6
bindsym $mod+Shift+7 move container to workspace $WS7
bindsym $mod+Shift+8 move container to workspace $WS8
bindsym $mod+Shift+9 move container to workspace $WS9
bindsym $mod+Shift+0 move container to workspace $WS10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

bindsym $mod+y exec notify-send yo
bindsym hyper+y exec notify-send yohyper

set $bg-color 	         #2f343f
set $inactive-bg-color   #2f343f
set $text-color          #f3f4f5
set $inactive-text-color #676E7D
set $urgent-bg-color     #E53935
set $primary             #9AC1CC

# window colors
#                       border              background         text                 indicator
client.focused          $bg-color           $primary          $text-color          $primary
client.unfocused        $inactive-bg-color $inactive-bg-color $inactive-text-color $bg-color
client.focused_inactive $inactive-bg-color $inactive-bg-color $inactive-text-color $bg-color
client.urgent           $urgent-bg-color    $urgent-bg-color   $text-color          $bg-color


bindsym $mod+d exec rofi -show run -theme launcher
#bindsym $mod+w exec rofi -show window -theme window_switcher
#bindsym $mod+s exec ~/.config/perso/rofi/scripts/social.sh


#Assigns some windows to specific workspaces or specific display type
#To find a window class, use:
for_window [class="^.*"] border pixel 1

for_window [class="Pavucontrol"] floating enable

for_window [class="pop-pulsemixer"] floating enable
for_window [class="pop-pulsemixer"] resize set 1920 350
for_window [class="pop-pulsemixer"] move window to position 0 0
for_window [class="pop-pulsemixer"] border pixel 0
for_window [class="pop-pulsemixer"] move scratchpad

for_window [class="pop-taskwarrior"] floating enable
for_window [class="pop-taskwarrior"] resize set 900 1080
for_window [class="pop-taskwarrior"] move window to position 1020 0
for_window [class="pop-taskwarrior"] border pixel 0
for_window [class="pop-taskwarrior"] move scratchpad

for_window [class="pop-finder"] floating enable
for_window [class="pop-finder"] resize set 1920 500
for_window [class="pop-finder"] move window to position 0 580
for_window [class="pop-finder"] border pixel 0
for_window [class="pop-finder"] move scratchpad

for_window [class="pop-music-finder"] floating enable
for_window [class="pop-music-finder"] resize set 1920 500
for_window [class="pop-music-finder"] move window to position 0 580
for_window [class="pop-music-finder"] border pixel 0
for_window [class="pop-music-finder"] move scratchpad

for_window [class="pop-calculator"] floating enable
for_window [class="pop-calculator"] resize set 900 1080
for_window [class="pop-calculator"] move window to position 1021 0
for_window [class="pop-calculator"] border pixel 0
for_window [class="pop-calculator"] move scratchpad

for_window [class="pop-pass"] floating enable
for_window [class="pop-pass"] resize set 1920 350
for_window [class="pop-pass"] move window to position 0 0
for_window [class="pop-pass"] border pixel 0
#for_window [class="pop-pulsemixer"] move scratchpad

assign [title="SuperCollider Help"] → $WS4
assign [class="SuperCollider"] → $WS4
assign [class="Messenger for Desktop"] → $WS9
for_window [class="Messenger for Desktop"] fullscreen enable
assign [class="Conky"] → $WS1

#Gaps config
smart_gaps on
smart_borders on

for_window [title="FocusVim"] smart_borders off
for_window [title="FocusVim"] smart_gaps off
#for_window [title="FocusVim"] fullscreen enable
for_window [title="FocusVim"] border pixel 0


gaps inner 20
gaps outer 0


bindsym $mod+Shift+x exec i3lock-fancy -g -f Oxygen-Mono

# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id /home/alex/.config/perso/pulseaudio/change-vol.sh +
bindsym XF86AudioLowerVolume exec --no-startup-id /home/alex/.config/perso/pulseaudio/change-vol.sh -
bindsym XF86AudioMute exec --no-startup-id /home/alex/.config/perso/pulseaudio/change-vol.sh 0
# Sreen brightness controls
bindsym XF86MonBrightnessUp exec sudo light -A 1 # increase screen brightness
bindsym XF86MonBrightnessDown exec sudo light -U 1 # decrease screen brightness

# Media player controls
bindsym XF86AudioNext exec ~/.config/perso/mpd/playseekplus.sh
bindsym Shift+XF86AudioNext exec ~/.config/perso/mpd/playnext.sh
bindsym XF86AudioPrev exec ~/.config/perso/mpd/playseekminus.sh
bindsym Shift+XF86AudioPrev exec ~/.config/perso/mpd/playprev.sh
bindsym XF86AudioPlay exec ~/.config/perso/mpd/playpause.sh

# Desktop screenshot
bindsym --release Print exec "scrot ~/images/screenshots/$(date --iso-8601='seconds').png -m -e 'xclip -selection c -t image/png < $f' ; notify-send 'Full desktop screenshot taken.'"
# # Choose window
bindsym --release Mod1+Sys_Req exec "import -window `xwininfo | awk '/.*Window id:.*/ {print $4}'` ~/images/screenshots/$(date --iso-8601='seconds').png; notify-send 'Window screenshot taken.'"
# # Selection screenshot
bindsym --release Shift+Print exec "scrot ~/images/screenshots/$(date --iso-8601='seconds').png -s -e 'xclip -selection c -t image/png < $f'; notify-send 'Screenshot of selected zone taken.'"

bindsym $mod+Shift+BackSpace move scratchpad
bindsym $mod+backslash scratchpad show


# exec urxvt -e byobu
exec --no-startup-id "/usr/bin/redshift"
exec --no-startup-id "/usr/bin/conky -c ~/.config/perso/conky/conky.conf"
exec --no-startup-id i3-msg 'workspace $WS2; exec st -e tmux new -s main'

#exec_always --no-startup-id $HOME/.config/polybar/launch.sh
#exec_always feh --bg-scale /home/alex/images/wallpapers/104787-pixel_sorting-glitch_art-aurorae.jpg
#exec_always feh --bg-scale /home/alex/images/wallpapers/_2x.gif
#exec_always feh --bg-scale /home/alex/images/wallpapers/244436.jpg
#exec_always feh --bg-scale file:///home/alex/images/wallpapers/nord_pixel_sorting-glitch_art-aurorae.jpg
exec_always feh --bg-scale file:///home/alex/images/wallpapers/current_wallpaper

exec_always --no-startup-id compton --config ~/.config/compton.conf

exec_always --no-startup-id dunst -conf ~/.config/dunst/dunstrc

exec --no-startup-id compton
exec --no-startup-id st -c pop-pulsemixer pulsemixer
exec --no-startup-id st -c pop-taskwarrior tmuxinator tasks
exec_always --no-startup-id st -c pop-finder ~/.config/perso/tools/finder
exec_always --no-startup-id st -c pop-music-finder ~/.config/perso/tools/finder-music
exec --no-startup-id st -c pop-pass ~/.config/perso/tools/passmanager
exec --no-startup-id ~/documents/apps/tools/st-calc -c pop-calculator insect

exec --no-startup-id "sleep 1; setxkbmap -option compose:ralt"
exec --no-startup-id sxhkd
exec_always --no-startup-id xset b off  #Sets system beep off
exec_always --no-startup-id setxkbmap -option "caps:hyper"

